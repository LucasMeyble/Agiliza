import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({

    input: {
        borderBottomWidth: 1,
        width: '100%',
        fontSize: 18,
        marginTop: 50,
        padding: 10,
        textAlign: 'center'
    }

})