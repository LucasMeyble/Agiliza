import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({

    container: {
        
    },
    button: {
        height: 40,
        width: 350,
        justifyContent: "center",
        alignItems: "center",
        borderRadius: 8,
    },
    textButton: {
        color: "#ffffff",
        fontSize: 14,
    }

})